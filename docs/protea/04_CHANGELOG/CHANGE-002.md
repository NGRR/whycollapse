# CHANGE-002 — acceso destacado del índice central

Estado: APPLY autorizado expresamente por Nicolás durante la ejecución; no exige otra confirmación.

Archivo: `index.html` en la raíz del repositorio.
Selector: `a.featured`.
Antes: botón azul superior hacia `./protea_vivov2/`, con descripción del prototipo anterior.
Después: botón azul superior hacia `./protea_vivo/`, con identificación `PROTEA_v02_REVIEW` y fecha 04 septiembre 2026.

Conservar el diseño del botón y el resto del índice byte a byte. No reinterpretar esta publicación como aprobación visual de la versión.

La instrucción incluye hacer commit al repositorio para que la versión pueda abrirse desde el índice central. Se actualiza `main` por avance directo únicamente si mantiene el padre comprobado; no usar force.
